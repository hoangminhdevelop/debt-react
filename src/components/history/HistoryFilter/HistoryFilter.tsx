import React, { HTMLProps } from 'react';

type HistoryFilterProps = HTMLProps<HTMLDivElement>;

export const HistoryFilter = (props: HistoryFilterProps) => {
  return <div {...props}>HistoryFilter</div>;
};
